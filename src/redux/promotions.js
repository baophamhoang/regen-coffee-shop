import { PROMOTIONS } from '../shared/promotions';

const promotionsReducer = (state = PROMOTIONS, action) => {
    switch(action.type){
        default: 
         return state;
    }
}

export default promotionsReducer;